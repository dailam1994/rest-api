// Declares Variables
export const Items = {
    type: 'object',
    properties: {
        id: { type: 'string' },
        content: { type: 'string' },
        isDone: { type: 'boolean', nullable: true }
    }
}

